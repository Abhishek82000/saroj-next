# Absolute origin used for canonicals, Open Graph URLs and the sitemap.
# Set this on every deploy — without it everything falls back to the live domain.
NEXT_PUBLIC_SITE_URL=https://www.sarojtextile.com
